import { ForbidDirective } from './forbid.directive';

describe('ForbidDirective', () => {
    it('should create an instance', () => {
        const directive = new ForbidDirective();
        expect(directive).toBeTruthy();
    });
});
