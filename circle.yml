machine:
  environment:
    NODE_ENV: circle_ci
  node:
    version: 7.10.0

dependencies:
  cache_directories:
    - "node_modules"

test:
  pre:
    - node_modules/protractor/bin/webdriver-manager update
  override:
    - npm run lint
    - npm test -- --single-run
    - npm run e2e
