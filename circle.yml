machine:
  environment:
    NODE_ENV: circle_ci
  node:
    version: 6.10.3

general:
  branches:
    only:
      - master
      - develop

dependencies:
  cache_directories:
    - "node_modules"

test:
  override:
    - npm test -- --single-run