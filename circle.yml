machine:
  environment:
    NODE_ENV: circle_ci
  node:
    version: 8.4.0

dependencies:
  cache_directories:
    - "node_modules"

test:
  override:
    - npm run lint
    - npm test -- --single-run
