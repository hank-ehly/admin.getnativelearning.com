machine:
  environment:
    NODE_ENV: circle_ci
  node:
    version: 6.10.3

dependencies:
  cache_directories:
    - "node_modules"

test:
  override:
    - npm run line
    - npm test -- --single-run
    - npm run e2e